<p id="notice"><%= notice %></p>

<div class="row">
    <div class="col-auto">
      <% if @location.address %>
        <%= render "/addresses/address_card", address: @location.address %>
      <% else %>
        no address
      <% end %>
    </div>
    <div class="col">
      <span class="h4"><%= @location.name %> / <%= @location.customer.full_name %>
    </div>
</div>
