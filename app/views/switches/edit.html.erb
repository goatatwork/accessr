<div class="row mt-4 mb-5">
    <div class="col">
      <span class="h4">Edit <%= @switch.name %></span>
    </div>
</div>

<%= turbo_frame_tag 'switches' do %>
  <%= render 'form', switch: @switch %>
<% end %>
