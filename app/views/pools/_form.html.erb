<%= form_with(model: [@subnet, @pool]) do |form| %>
  <% if pool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pool.errors.count, "error") %> prohibited this pool from being saved:</h2>

      <ul>
        <% pool.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :start_ip %>
    <%= form.text_field :start_ip %>
  </div>

  <div class="field">
    <%= form.label :end_ip %>
    <%= form.text_field :end_ip %>
  </div>

  <div class="field">
    <%= form.label :subnet_id %>
    <%= form.text_field :subnet_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
