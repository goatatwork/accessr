require "test_helper"

class AddressesControllerTest < ActionDispatch::IntegrationTest
  # setup do
  #   @address = addresses(:one)
  # end

  # test "should get index" do
  #   skip "Skipping address_controller tests."
  #   get addresses_url
  #   assert_response :success
  # end

  # test "should get new" do
  #   skip "Skipping address_controller tests."
  #   get new_address_url
  #   assert_response :success
  # end

  # test "should create address" do
  #   skip "Skipping address_controller tests."
  #   assert_difference('Address.count') do
  #     post addresses_url, params: { address: { address1: @address.address1, address2: @address.address2, addressable_id: @address.addressable_id, addressable_type: @address.addressable_type, city: @address.city, postal_code: @address.postal_code, state: @address.state } }
  #   end

  #   assert_redirected_to address_url(Address.last)
  # end

  # test "should show address" do
  #   skip "Skipping address_controller tests."
  #   get address_url(@address)
  #   assert_response :success
  # end

  # test "should get edit" do
  #   skip "Skipping address_controller tests."
  #   get edit_address_url(@address)
  #   assert_response :success
  # end

  # test "should update address" do
  #   skip "Skipping address_controller tests."
  #   patch address_url(@address), params: { address: { address1: @address.address1, address2: @address.address2, addressable_id: @address.addressable_id, addressable_type: @address.addressable_type, city: @address.city, postal_code: @address.postal_code, state: @address.state } }
  #   assert_redirected_to address_url(@address)
  # end

  # test "should destroy address" do
  #   skip "Skipping address_controller tests."
  #   assert_difference('Address.count', -1) do
  #     delete address_url(@address)
  #   end

  #   assert_redirected_to addresses_url
  # end
end
