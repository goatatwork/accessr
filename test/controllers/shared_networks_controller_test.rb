require "test_helper"

class SharedNetworksControllerTest < ActionDispatch::IntegrationTest
  # setup do
  #   @shared_network = shared_networks(:one)
  # end

  # test "should get index" do
  #   skip "Skipping shared_networks tests."
  #   get shared_networks_url
  #   assert_response :success
  # end

  # test "should get new" do
  #   skip "Skipping shared_networks tests."
  #   get new_shared_network_url
  #   assert_response :success
  # end

  # test "should create shared_network" do
  #   skip "Skipping shared_networks tests."
  #   assert_difference('SharedNetwork.count') do
  #     post shared_networks_url, params: { shared_network: { dhcp_server_id: @shared_network.dhcp_server_id, name: @shared_network.name, relayed_from_ip: @shared_network.relayed_from_ip } }
  #   end

  #   assert_redirected_to shared_network_url(SharedNetwork.last)
  # end

  # test "should show shared_network" do
  #   skip "Skipping shared_networks tests."
  #   get shared_network_url(@shared_network)
  #   assert_response :success
  # end

  # test "should get edit" do
  #   skip "Skipping shared_networks tests."
  #   get edit_shared_network_url(@shared_network)
  #   assert_response :success
  # end

  # test "should update shared_network" do
  #   skip "Skipping shared_networks tests."
  #   patch shared_network_url(@shared_network), params: { shared_network: { dhcp_server_id: @shared_network.dhcp_server_id, name: @shared_network.name, relayed_from_ip: @shared_network.relayed_from_ip } }
  #   assert_redirected_to shared_network_url(@shared_network)
  # end

  # test "should destroy shared_network" do
  #   skip "Skipping shared_networks tests."
  #   assert_difference('SharedNetwork.count', -1) do
  #     delete shared_network_url(@shared_network)
  #   end

  #   assert_redirected_to shared_networks_url
  # end
end
